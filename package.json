{
  "name": "patrizio-acquadro-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "security:check": "npm run build && node scripts/security-baseline-check.mjs"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
